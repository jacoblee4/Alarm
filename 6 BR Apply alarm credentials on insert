Name: 6 BR Apply alarm credentials on insert

After Insert

When to run: After Insert (Order 100, so it runs after BR 1)

Filter condition:

Issue type is Alarm Panel Request
Request type is add
Alarm Password is empty

Script:

javascript
(function executeRule(current, previous /*null when async*/ ) {
    new x_banun_bunow_al_0.AlarmCodeHelper().applyCredentials(current.getUniqueValue());
})(current, previous);
