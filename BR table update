condition: current.alarm_password.changes() && !current.alarm_password.nil()

script:
(function executeRule(current, previous /*null when async*/ ) {
	// trigger the script include to update other passwords for same user
    new x_banun_bunow_al_0.AlarmCodeHelper().propagateCredentials(current.getUniqueValue());

})(current, previous);
